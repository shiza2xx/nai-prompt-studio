# NAI Prompt Studio v0.6.5

## Faster reusable artist-card previews

- v0.6.5 speeds up reusable artist-card loading with a bounded two-tier preview cache, loads original artist images lazily on hover, and keeps custom and user cards untouched without recompressing their images.

The Windows installer is unsigned, so Microsoft SmartScreen may show an unknown-publisher warning.
